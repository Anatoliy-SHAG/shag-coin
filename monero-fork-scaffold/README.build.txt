Build notes (high level)
- Monero build requires: cmake, gcc or MSVC toolchain, boost, openssl, libunwind (optional), pkg-config
- On Windows use MSYS2 or Visual Studio with correct environment.
- For RandomX you may need a recent compiler with JIT support.
